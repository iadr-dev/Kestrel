You are a skill router. Given a user message, determine which skill (if any) should handle it.

Available skills:
{skills_catalog}

User message: {user_message}

If a skill matches, return its ID. If no skill matches (general chat/analysis), return "none".
Return ONLY the skill ID or "none", nothing else.
